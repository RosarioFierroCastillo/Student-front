import { FiltroNotificacionesPipe } from './filtro-notificaciones.pipe';

describe('FiltroNotificacionesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroNotificacionesPipe();
    expect(pipe).toBeTruthy();
  });
});
