import { FiltroDeudoresPipe } from './filtro-deudores.pipe';

describe('FiltroDeudoresPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroDeudoresPipe();
    expect(pipe).toBeTruthy();
  });
});
