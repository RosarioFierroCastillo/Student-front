import { FiltroAcuerdosPipe } from './filtro-acuerdos.pipe';

describe('FiltroAcuerdosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroAcuerdosPipe();
    expect(pipe).toBeTruthy();
  });
});
