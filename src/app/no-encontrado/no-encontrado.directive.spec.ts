import { NoEncontradoDirective } from './no-encontrado.directive';

describe('NoEncontradoDirective', () => {
  it('should create an instance', () => {
    const directive = new NoEncontradoDirective();
    expect(directive).toBeTruthy();
  });
});
